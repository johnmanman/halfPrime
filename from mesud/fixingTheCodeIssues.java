
// TODO: Fix the code issues before anything
					
          //Address variables were not proberly assigned, I had to assign them;
					//one of the Address and the Student classes were not overriding the equals() and hashcode();
					//id was given the geters and setters methods, which I had to correct it; avoided the encapsulations there);
